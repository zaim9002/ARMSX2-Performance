package com.armsx2.i18n

/**
 * Empty by design: the Play build ships no frame-generation strings at all. Keeping the name
 * identical to the github table is what lets [EN] merge one or the other without any caller
 * knowing which flavour it is compiled into.
 */
internal val LSFG_EN: Map<String, String> = emptyMap()
